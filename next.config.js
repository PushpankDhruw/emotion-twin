// @ts-check

/** @type {import('next').NextConfig} */
const nextConfig = {
	// * Demo mode:
	/* reactStrictMode: true,
	images: {
	  domains: ['cdn.sanity.io'],
	  loader: 'custom',
	}, */
  }
  
  module.exports = nextConfig
  